<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.laruletadelafortuna">

    <application
        android:name=".atributos"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".actividadConfiguracion"></activity>
        <activity android:name=".actividadInformacion"></activity>
        <activity android:name=".actividad_SeleccionNJugadores"></activity>
        <activity android:name=".actividadJugar"></activity>
        <activity android:name=".actividadJugar_Tablero"></activity>
        <activity android:name=".actividadGanar"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>